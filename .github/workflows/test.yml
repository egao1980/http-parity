name: test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  SBCL_VERSION: 2.6.7
  ROSWELL_VERSION: "26.02.116"
  HTTP_PARITY_LIVE: "1"
  HTTP_PARITY_BACKEND: "async"
  HTTP_PARITY_FIXTURE: "1"
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            event_backend: libuv
          - os: ubuntu-latest
            event_backend: libev
          - os: macos-latest
            event_backend: libuv
          - os: windows-latest
            event_backend: libuv
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
      packages: read
    env:
      HTTP_ASYNC_EVENT_BACKEND: ${{ matrix.event_backend }}
      HTTP_PARITY_EVENT_BACKEND: ${{ matrix.event_backend }}
      HTTP_PARITY_LIVE: "1"
      HTTP_PARITY_FIXTURE: "1"
      HTTP_PARITY_BACKEND: "async"
    steps:
      - uses: actions/checkout@v5
      - uses: egao1980/cl-repository/.github/actions/setup-client@main
      - uses: egao1980/cl-repository/.github/actions/setup-roswell@main
        with:
          sbcl-version: ${{ env.SBCL_VERSION }}
          roswell-version: ${{ env.ROSWELL_VERSION }}
      - uses: egao1980/cl-repository/.github/actions/ci@main
        with:
          phase: install
      - uses: egao1980/cl-repository/.github/actions/ci@main
        with:
          phase: test
